{
  "name": "rocket-alert",
  "version": "1.7.0",
  "description": "Real-time red alerts in Israel",
  "author": "Erez Nagar (erez.nagar@gmail.com)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ErezNagar/rocket-alert"
  },
  "homepage": "https://ereznagar.github.io",
  "keywords": [
    "rockets",
    "red alert",
    "codered",
    "code-red",
    "alerts",
    "israel"
  ],
  "dependencies": {
    "@ant-design/charts": "^1.4.3",
    "@mapbox/polyline": "^1.2.1",
    "ajv": "^8.17.1",
    "date-fns": "^2.22.1",
    "query-string": "^7.0.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "^5.0.1",
    "web-vitals": "^1.1.2",
    "wretch": "^1.7.4"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^5.13.0",
    "@testing-library/react": "^11.2.7",
    "@testing-library/user-event": "^12.8.3",
    "antd": "^4.16.12",
    "date-fns-tz": "^1.3.6",
    "gh-pages": "^6.3.0",
    "jest": "27.4.7",
    "jest-watch-typeahead": "^0.6.5",
    "typescript": "^4.3.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "preversion": "npm test",
    "version": "npm run build && git add *",
    "postversion": "git push && git push --tags && rm -rf build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "docker-build": "VERSION=1.0.0 && IMAGE_NAME=dchevallier/rocket-alert && docker buildx build --no-cache --platform linux/amd64,linux/arm64 -t $IMAGE_NAME:test-latest -t $IMAGE_NAME:test-$VERSION . --push",
    "docker-run": "VERSION=1.0.0 && IMAGE_NAME=dchevallier/rocket-alert && docker run --rm -it -p 80:3000 $IMAGE_NAME:test-$VERSION"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
