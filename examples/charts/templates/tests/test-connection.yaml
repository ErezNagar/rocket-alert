apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rocket-alert.fullname" . }}-test-connection"
  labels:
    {{- include "rocket-alert.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rocket-alert.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
