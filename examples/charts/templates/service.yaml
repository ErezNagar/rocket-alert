apiVersion: v1
kind: Service
metadata:
  name: {{ include "rocket-alert.fullname" . }}
  labels:
    {{- include "rocket-alert.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rocket-alert.selectorLabels" . | nindent 4 }}
