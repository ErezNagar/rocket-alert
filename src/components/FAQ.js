// import PropTypes from "prop-types";

const FAQ = () => (
  <section className="section faq">
    <h2>Frequently Asked Questions</h2>
  </section>
);

FAQ.propTypes = {};

FAQ.defaultProps = {};

export default FAQ;
